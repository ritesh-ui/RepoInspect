name: RepoGuard Security Scan

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  scan:
    name: RepoGuard AI Audit
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
          python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run RepoGuard Scan
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      run: |
        # Scan the current directory
        # Fails the build if High or Critical vulnerabilities are found
        python scan_repo.py . --fail-on High --markdown REPO_GUARD_REPORT.md

    - name: Upload Report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: repoguard-report
        path: REPO_GUARD_REPORT.md
